var gui = new dat.GUI();
var controls = {
    rotationSpeed: 0.01,
    lightIntensity: 1.0
};

gui.add(controls, 'rotationSpeed', 0, 0.1);
gui.add(controls, 'lightIntensity', 0, 2).onChange(function (value) {
    gl.uniform1f(lightColorLocation, value);
});
